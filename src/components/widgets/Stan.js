import React from 'react';
import {Image} from '../elements';

const Stan = props => {
  return (
    <div>
      <Image
        src="/stan.png"
        height={500}
      />
    </div>
  );
};

export default Stan;
